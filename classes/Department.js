class Department {

    constructor(name) {
        this.name = name;
        // pulling id will be from pulling db
        // this.id = db.query(select department.id from department)
    }

    viewAllDepartments = () => {

    }

    addDepartment = () => {
        
    }
}

module.exports = Department;